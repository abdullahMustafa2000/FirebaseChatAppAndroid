package com.example.mychatapplication.pojo.DAOs;
/*
import androidx.lifecycle.LiveData;
import androidx.room.Dao;
import androidx.room.Delete;
import androidx.room.Insert;
import androidx.room.Query;
import androidx.room.Update;

import com.example.mychatapplication.Models.Chat;
import com.example.mychatapplication.pojo.Tables.ChatsTables;

import java.util.ArrayList;
@Dao
public interface ChatListDao {

    @Insert
    void insertRoom(ChatsTables chatsTables);

    @Update
    void updateChatRoom(ChatsTables chatsTables);

    @Query("Select * from chatTable where roomId= :roomId")
    LiveData<ChatsTables> CHATS_TABLES_LIVE_DATA(String roomId);

    @Query("Delete from chatTable where roomId= :roomId")
    void deleteRoom(String roomId);

    @Query("Update chatTable set ChatList= :chatArrayList where roomId= :roomId")
    void updateChat(ArrayList<Chat> chatArrayList, String roomId);


}*/